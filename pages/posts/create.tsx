import * as React from 'react';

export interface CreatePostPagePropsProps {
}

export default function CreatePostPageProps (props: CreatePostPagePropsProps) {
  return (
    <div>
      Create a new post
    </div>
  );
}
